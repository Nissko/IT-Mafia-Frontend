import React from "react";
import './header-shop.css'

const HeaderShop = () => {
return(
    <header className="site-header">
        <h1 className="site-title">Магазин</h1>
    </header>

    );
}

export default HeaderShop;